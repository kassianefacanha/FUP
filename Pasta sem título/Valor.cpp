#include<iostream> 
#include<string>

using namespace std;

int main() {

            int x;
            cout << "Digite um Valor";
            cin >> x;
            if (x>0) {
              cout << " O valor vai ser positivo" ;
                       
                       if (x==0) 
                       cout << " O valor será Nulo" ;
 
                        if (x<0)
                       cout << " O valor será Negativo" ; 
                       }
                   
            return 0;
}
